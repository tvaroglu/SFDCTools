#bulk and #sf API auth methods are imported from a separate Python module within local repo, for creds masking.

Parameters for both methods are commented within the appropriate modules, for easy reference.